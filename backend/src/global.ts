import { GlobalInfo } from "./type";

const Globalinfo: GlobalInfo = {
  info: {
    mos: { schema: null, notifications: null, routes: new Map() },
    sbp: { schema: null, notifications: null, routes: new Map() },
  },
  countuser: 0,
};

export default Globalinfo;
